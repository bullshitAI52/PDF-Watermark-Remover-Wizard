# 🎨 PDF Watermark Remover Wizard (Pro V0.3)

基于 Python 的 PDF 批量去水印工具。采用取证级分析与多引擎处理，支持可视化对象点选、区域框选、矢量清理及 AI 修补。

## 🌟 核心亮点 (Key Features)

1.  **🖥️ 可视化 Pro 界面 (V3)**: 所见即所得，双击水印即可删除，支持**自由区域框选**（无对象也能涂除）。
2.  **🌍 全书同步匹配 (V3 加速)**: 后台线程扫描、每页只解析一次、可取消；支持严格/宽松/仅内容匹配。
3.  **🪄 AI 图像修补**: 针对扫描件、复杂背景，利用云端大模型进行像素级修复。
4.  **☢️ 矢量核弹模式**: 强力剥离 PDF 底层矢量路径、网格、灰度线条。
5.  **⚡ 极简工作流**: 放入文件 -> 一键启动 -> 可视化标记 -> 快速导出。

> **版本说明**: 菜单 **1 = V3 优化版（推荐）**，**2 = V2 经典版**（原逻辑完整保留，互不影响）。

## 📁 目录结构

- **`core/`**: 核心处理引擎（ObjectEngine, VectorKiller）。
- **`gui_app/`**: V2 专业版桌面 UI 界面。
- **`image_mode_pic_watermark/`**: 离线图片模式与 AI 修补逻辑。
- **`utils/`**: 统一配置管理（API Key）与文件工具。
- **`input/`**: 📥 待处理文件存放处。
- **`output/`**: 📤 处理结果输出目录。

## 🚀 快速启动 (Mac)

直接双击运行根目录下的：
**`一键启动_Mac.command`**

> 脚本会自动创建虚拟环境并安装所需依赖（如 `pikepdf`, `PyMuPDF`, `customtkinter` 等）。

## 🛠️ 模式说明

| 编号 | 模式名称 | 适用场景 |
| :--- | :--- | :--- |
| **1** | **可视化智能清理 V3 (推荐)** | 后台同步、自由区域、按页导出、键盘快捷键。 |
| **2** | **可视化智能清理 V2 (经典)** | 与旧版完全一致的点选/框选/同步流程。 |
| **4** | **AI 图像修补 (Cloud)** | 扫描件 PDF、背景极其复杂的图片水印。 |
| **5** | **核弹模式 (Vector)** | 普通方法删不掉的底纹、边框、矢量线条。 |
| **6** | **本地图片模式 (Offline)** | 纯图片、扫描件，要求处理速度快且无需联网。 |
| **7** | **系统设置** | 配置阿里云 DashScope API Key。 |

### V3 快捷键

| 按键 | 作用 |
| :--- | :--- |
| `←` / `→` | 上一页 / 下一页 |
| `Cmd/Ctrl + S` | 导出到 output |
| `Esc` | 取消全书同步 |
| `+` / `-` | 缩放 |

## 📦 环境依赖

程序会自动安装依赖，但建议确保系统已安装 `poppler`（用于 PDF 转图）：
```bash
brew install poppler
```

---
*声明：本工具仅供个人学习研究使用，严禁用于商业或非法用途。*

---

## 🖥️ GUI 可视化界面使用手册

启动 `gui_app/main_v2.py` 后，你会看到三栏布局：左侧侧边栏、中央画布、顶部状态栏。

### 基本流程

```
放入 PDF → 打开文件 → 标记水印 → 全书同步(可选) → 导出
```

### 第一步：打开 PDF

1. 左侧 **📁 Input Files** 列表会显示 `input/` 文件夹下的所有 PDF
2. **点击**文件名，文件加载到中央画布

### 第二步：标记水印（删除指定页）

**对象点选模式（默认）**

- 页面上被检测到的对象会用 **蓝色框** 标出
- **鼠标悬停** → 蓝色框变红色
- **双击**红色框 → 标记为要删除的水印，框变**红色填充**
- 再**双击**已标记的 → **取消标记**

**区域框选模式**

- 点击侧边栏 **⬜ 区域框选** 切换到框选模式
- 在画布上**按住拖拽**拉出一个绿色虚线框
- 松开鼠标 → 框内所有对象一次标记

> 💡 **只删除某一页**：只在该页标记，不要点"全书同步"，然后直接导出即可。

### 第三步：全书同步删除（所有页）

- 先在**某一页**标记 1-2 个水印对象
- 点击 **🌍 全书同步删除**
- 弹出确认框 → 点击 **确认同步**
- 程序会在全部页面中查找**相同位置、相同大小、相同内容的**对象并标记
- 状态栏显示进度：`⏳ 全书同步中 (45/180 页)...`
- 完成后自动刷新页面，同步标记的对象会显示红色填充

> ⚠️ 匹配条件比较严格：位置±5px、大小±2px、文字完全一致。如果水印在不同页位置有偏移，可以只手动逐页标记。

### 第四步：导出

- **📥 导出到 output 文件夹** → 自动保存到 `output/原文件名_cleaned.pdf`
- **💾 另存为…** → 自行选择保存位置和文件名

### 常见问题

**Q: 检测不到水印（页面上没有蓝色框）？**  
A: 水印可能是扫描件中的图片，不是可解析的 PDF 对象。这种情况切换到**区域框选模式**，手动圈出水印区域。

**Q: 全书同步找不到任何匹配？**  
A: 水印在不同页的位置/大小有变化时，同步匹配可能会失败。可以逐页手动双击标记后导出。

**Q: 页面水印太多，框选很慢？**  
A: 先双击标记一两个关键水印，再用全书同步自动匹配所有页，速度最快。

### 多类水印场景：不同页不同水印

如果 PDF 中不同页的水印不一样（比如第一页是 A 水印，后面是 B 水印），也只需一次同步：

1. **翻到第一页** → 双击标记 A 水印
2. **翻到后面任意一页** → 双击标记 B 水印
3. 此时你已标记了**两类**水印模板
4. 点 **🌍 全书同步删除**
5. 程序会用**所有已标记的模板**在全篇搜索匹配
6. 一次导出，两类水印都清理掉

> 全书同步会遍历你标记的**所有**对象作为模板，不局限于某一页。在不同页标记的不同水印，都会分别去全文匹配。
